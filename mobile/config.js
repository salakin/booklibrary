// Single place to point the app at your backend.
// Use your machine's LAN IP (not "localhost") when testing on a physical device,
// since the device can't resolve the dev machine's localhost.
export const API_BASE_URL = 'http://192.168.0.109:8000';
